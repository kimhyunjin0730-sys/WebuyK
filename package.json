{
  "name": "we-buy-k",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "db:push": "pnpm --filter @wbk/api run db:push",
    "db:seed": "pnpm --filter @wbk/api run db:seed"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.7.0"
}
